/*
 * Slack.gs
 * Handles sending messages to Slack via Incoming Webhook.
 *
 * Contains:
 *   - SLACK_WEBHOOK_URL constant
 *   - sendSlackMessage(text) → posts to #team-ops-vert
 *   - testSlackMessage() → sends a test message for verification
 *
 * Setup:
 *   1. Create a Slack Incoming Webhook for #team-ops-vert
 *   2. Paste the webhook URL in the constant below
 *
 * Called by Reports.gs after the weekly report is built.
 */

// Slack webhook functions will be built here
